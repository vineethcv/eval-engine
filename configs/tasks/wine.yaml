name: wine_recommendation

dataset_path: dataset.json
rubric_path: rubric.json

description: "Wine recommendation evaluation task (V1 reference task)"

# Placeholder for future (not used yet)
judges:
  enabled: true